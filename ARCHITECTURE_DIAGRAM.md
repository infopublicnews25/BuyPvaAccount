# 📊 System Architecture & Data Flow Diagram

## Complete User Journey Visualization

```
USER INTERFACE FLOW
═════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────┐
│                         🔴 START: SIGNUP.HTML                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ FORM INPUTS:                                                       │ │
│  │  • Full Name        → Text input                                   │ │
│  │  • Email            → Email input (validated format)              │ │
│  │  • Phone            → Tel input                                   │ │
│  │  • Country          → Select dropdown                             │ │
│  │  • Password         → Password input (min 6 chars)                │ │
│  │  • Confirm Password → Must match password                         │ │
│  │                                                                    │ │
│  │ VALIDATIONS (Frontend):                                          │ │
│  │  ✓ All required fields filled                                    │ │
│  │  ✓ Email format valid                                            │ │
│  │  ✓ Password >= 6 characters                                      │ │
│  │  ✓ Passwords match                                               │ │
│  │                                                                    │ │
│  │ [Create Account Button] ──────────────┐                          │ │
│  └────────────────────────────────────────┼──────────────────────────┘ │
└──────────────────────────────────────────┼─────────────────────────────┘
                                           │
                                           ▼
                        ┌──────────────────────────────────┐
                        │  BACKEND: /api/signup            │
                        │  ─────────────────────────────   │
                        │ 1. Validate all fields           │
                        │ 2. Check email not duplicate     │
                        │ 3. Hash password (bcrypt)        │
                        │ 4. Create user record            │
                        │ 5. Save to registered_users.json │
                        │                                  │
                        │ RESPONSE: {                      │
                        │   success: true,                 │
                        │   user: { email, fullName, ... } │
                        │ }                                │
                        └──────────────────────────────────┘
                                           │
                    ┌──────────────────────┴──────────────────────┐
                    │                                              │
                    ▼                                              ▼
         ✅ Success                                     ❌ Error
         ┌─────────────────────────────────┐          │
         │                                 │     Return error:
         │ Redirect to:                    │     • Duplicate email
         │ signup-success.html             │     • Invalid email
         │ ?email=user@example.com         │     • Short password
         │                                 │     • Server error
         └────────────┬────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                  🟢 SUCCESS PAGE: SIGNUP-SUCCESS.HTML                  │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      ✓ (Animated checkmark)                      │  │
│  │                    Account Created!                              │  │
│  │                 Welcome to BuyPvaAccount                         │  │
│  │                                                                  │  │
│  │  ┌──────────────────────────────────────────────────────────┐  │  │
│  │  │  Registered Email                                        │  │  │
│  │  │  ───────────────                                         │  │  │
│  │  │                                                          │  │  │
│  │  │  testdec14@example.com                                  │  │  │
│  │  └──────────────────────────────────────────────────────────┘  │  │
│  │                                                                  │  │
│  │  Your account has been created successfully.                   │  │
│  │  Please login with your email and password to continue.        │  │
│  │                                                                  │  │
│  │  [Continue to Login →]                                         │  │
│  │                                                                  │  │
│  │  🔒 Security Tip: Never share your password with anyone.       │  │
│  │                                                                  │  │
│  └──────────────────────────┬───────────────────────────────────────┘  │
└─────────────────────────────┼────────────────────────────────────────────┘
                              │
                              │ Click Button or Manual Navigate
                              │
                              ▼
        ┌──────────────────────────────────────────┐
        │ Redirect to:                             │
        │ login.html?email=testdec14@example.com   │
        │                                          │
        │ Email parameter passed in URL            │
        └──────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    🔵 LOGIN PAGE: LOGIN.HTML                           │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                      Welcome Back                                 │ │
│  │                 Login to access your account                      │ │
│  │                                                                   │ │
│  │  ┌───────────────────────────────────────────────────────────┐   │ │
│  │  │ ✅ Account created! Login with testdec14@example.com      │   │ │
│  │  │    (Green info box with email highlighted)               │   │ │
│  │  └───────────────────────────────────────────────────────────┘   │ │
│  │                                                                   │ │
│  │  Email Address                                                   │ │
│  │  ┌──────────────────────────────────────────────────────────┐   │ │
│  │  │ testdec14@example.com  ✓ (PRE-FILLED!)                 │   │ │
│  │  └──────────────────────────────────────────────────────────┘   │ │
│  │                                                                   │ │
│  │  Password                                                        │ │
│  │  ┌──────────────────────────────────────────────────────────┐   │ │
│  │  │ ••••••••••••  [Auto-focused, ready for typing]         │   │ │
│  │  └──────────────────────────────────────────────────────────┘   │ │
│  │                                                                   │ │
│  │              [Login]                                             │ │
│  │                                                                   │ │
│  │  Don't have an account? Sign up here                            │ │
│  │                      Forgot password?                            │ │
│  │                                                                   │ │
│  └───────────────┬──────────────────────────────────────────────────┘ │
└──────────────────┼────────────────────────────────────────────────────┘
                   │
        User enters password & clicks Login
                   │
                   ▼
        ┌──────────────────────────────┐
        │  BACKEND: /api/login         │
        │  ─────────────────────────   │
        │ 1. Find user by email        │
        │ 2. Compare passwords (bcrypt)│
        │ 3. Return user data          │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
   ✅ Valid                      ❌ Invalid
   ┌──────────────────────┐     │
   │ Save to localStorage:│     Return error:
   │ client_auth = {      │     • Wrong password
   │   email,             │     • User not found
   │   fullName,          │
   │   phone,             │
   │   country,           │
   │   authType,          │
   │   loginTime          │
   │ }                    │
   │                      │
   │ Redirect to:         │
   │ profile.html         │
   └──────────┬───────────┘
              │
              ▼
┌──────────────────────────────────────────────────────────────────────┐
│              🟠 PROFILE PAGE: PROFILE.HTML                          │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Left Sidebar:                 Main Content:                 │  │
│  │  ───────────────               ─────────────                 │  │
│  │  • Avatar                       • User Profile               │  │
│  │  • User Name                    • My Orders                  │  │
│  │  • Menu Options                 • Account Settings           │  │
│  │  • Logout                       • Edit Profile               │  │
│  │                                                               │  │
│  │  All data loaded from localStorage client_auth              │  │
│  │  Shows: Name, Email, Phone, Country, Orders                │  │
│  └──────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Backend Database Flow

```
DATABASE: registered_users.json
════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────┐
│ User Record Structure:                                               │
│                                                                      │
│ {                                                                    │
│   "fullName": "Test User December",                                │
│   "email": "testdec14@example.com",                                │
│   "phone": "+8801700000099",                                       │
│   "country": "Bangladesh",                                         │
│   "authType": "email",                                             │
│   "passwordHash": "$2b$12$QXEqU5.todLWGVYbHDQlOO4WS69mphRiEjk...",│
│   "passwordMigrated": true,                                        │
│   "passwordMigratedAt": "2025-12-14T10:30:00.000Z",              │
│   "createdAt": "2025-12-14T10:25:00.000Z"                        │
│ }                                                                  │
│                                                                    │
│ OR (Auto-created during checkout):                                │
│                                                                    │
│ {                                                                  │
│   "fullName": "John Doe",                                        │
│   "email": "john.auto@example.com",                              │
│   "phone": "+8801700000333",                                     │
│   "country": "Bangladesh",                                       │
│   "authType": "email",                                           │
│   "autoCreated": true,  ← Marked as auto-created                │
│   "passwordHash": "$2b$12$...",                                 │
│   "passwordMigrated": true,                                      │
│   "passwordMigratedAt": "2025-12-14T10:30:00.000Z",            │
│   "createdAt": "2025-12-14T10:25:00.000Z"                      │
│ }                                                                 │
│                                                                   │
└──────────────────────────────────────────────────────────────────────┘

Password Storage Security:
═════════════════════════════════════════════════════════════════════════

User Input:           "TestPass123!"
                              │
                              ▼
                    ┌────────────────────┐
                    │  BCRYPT HASHING    │
                    │  12 salt rounds    │
                    └────────────────────┘
                              │
                              ▼
Stored in DB:   "$2b$12$QXEqU5.todLWGVYbHDQlOO4WS69mphRiEjkQMXUi6WCdJdplX9X3e"

During Login:
            Input Password
                    │
                    ▼
            Hash & Compare
                    │
                    ▼
    ┌─────────────┴─────────────┐
    │                           │
    ✅ Match → Login OK    ❌ No Match → Rejected
```

---

## Data Storage Locations

```
FILESYSTEM
═════════════════════════════════════════════════════════════════════════

Project Root
│
├── 📄 signup.html              ← User registration form
├── 📄 signup-success.html      ← Success confirmation (NEW!)
├── 📄 login.html               ← Login form (UPDATED)
├── 📄 forgot-password.html     ← Password reset
├── 📄 profile.html             ← User profile display
│
├── 📄 registered_users.json    ← All users database
│   └─ Contains: [{email, fullName, passwordHash, ...}, ...]
│
├── backend/
│   ├── 📄 server.js            ← Backend API server
│   │   ├─ POST /api/signup
│   │   ├─ POST /api/login
│   │   ├─ POST /api/auto-register
│   │   ├─ POST /api/send-reset-code
│   │   └─ POST /api/reset-password
│   └── 📄 package.json         ← Dependencies

CLIENT-SIDE STORAGE (Browser)
═════════════════════════════════════════════════════════════════════════

localStorage:
  • client_auth: {
      email,
      fullName,
      phone,
      country,
      authType,
      loginTime
    }

sessionStorage:
  • signupEmail: (temporary, for passing between pages)

URL Parameters:
  • signup-success.html?email=user@example.com
  • login.html?email=user@example.com
```

---

## API Endpoint Reference

```
ALL ENDPOINTS
═════════════════════════════════════════════════════════════════════════

1. POST /api/signup
   ├─ Request:
   │  └─ { fullName, email, phone, country, password, authType }
   ├─ Validation: Email unique, password >= 6, all required
   ├─ Action: Hash password (bcrypt), save user
   └─ Response: { success: true, user: {...} }

2. POST /api/login
   ├─ Request:
   │  └─ { email, password }
   ├─ Validation: User exists, password matches
   ├─ Action: Compare bcrypt password
   └─ Response: { success: true, user: {...} }

3. POST /api/auto-register
   ├─ Request:
   │  └─ { fullName, email, phone, country }
   ├─ Used by: Checkout.html for non-members
   ├─ Action: Create user with temp password
   └─ Response: { success: true, isNew: true, user: {...} }

4. POST /api/send-reset-code
   ├─ Request:
   │  └─ { email, code }
   ├─ Used by: forgot-password.html
   ├─ Action: Send code via email (or console fallback)
   └─ Response: { success: true }

5. POST /api/reset-password
   ├─ Request:
   │  └─ { email, password }
   ├─ Used by: forgot-password.html after code verify
   ├─ Action: Update password (bcrypt hash)
   └─ Response: { success: true }
```

---

## Security Architecture

```
PASSWORD SECURITY
═════════════════════════════════════════════════════════════════════════

                    User Password
                    "TestPass123!"
                            │
                            ▼
        ┌─────────────────────────────────────┐
        │   BCRYPT ALGORITHM (12 salt rounds)  │
        │   ─────────────────────────────────  │
        │   • Irreversible encryption          │
        │   • Each hash is unique              │
        │   • Time-consuming (slows brute force)│
        │   • Industry standard                │
        └─────────────────────────────────────┘
                            │
                            ▼
        $2b$12$QXEqU5.todLWGVYbHDQlOO4WS69...
        (Stored in Database - NEVER PLAIN TEXT)
                            │
                ┌───────────┴───────────┐
                │                       │
        During Login:           During Password Reset:
        User enters password    User enters new password
                │                       │
                ▼                       ▼
        Compare with hash   Create new hash
                │                       │
    ┌───────────┴───────────┐          │
    │                       │          ▼
✅ Match OK         ❌ No Match    Update database
  Allow login        Reject          with new hash
```

---

## Complete Request/Response Flow

```
SIGNUP FLOW
═════════════════════════════════════════════════════════════════════════

USER BROWSER                          BACKEND SERVER
────────────────                      ───────────────

signup.html
│
├─ Validate form (frontend)
│
├─ Send POST /api/signup ─────────────→ Receive request
│  { fullName, email,                  Check validations
│    phone, country,                   - Email valid?
│    password, authType }              - Email duplicate?
│                                      - Password >= 6?
│
│                                      Bcrypt hash password
│
│                                      Create user record
│                                      Save to JSON file
│
│                    ←──────────────── Response:
│                                     { success: true,
Response.ok = true  ✅                  user: {...} }
│
Redirect to
signup-success.html
?email=user@...
│
Display success page
Show email: user@example.com
│
User clicks "Continue"
│
Redirect to
login.html
?email=user@...
│
Email pre-filled
Info box shows


LOGIN FLOW
═════════════════════════════════════════════════════════════════════════

USER BROWSER                          BACKEND SERVER
────────────────                      ───────────────

login.html
│
├─ Email pre-filled
├─ User enters password
│
├─ Send POST /api/login ─────────────→ Receive request
│  { email, password }                 Find user by email
│                                      Get stored hash
│
│                                      Compare password
│                                      with bcrypt hash
│
│                    ┌─────────────────────────┐
│                    │ Passwords match? YES/NO │
│                    └─────────────────────────┘
│                            │
│                ┌───────────┴───────────┐
│                │                       │
│    ←──── Response OK ✅         Response 401 ❌
│    { success: true,              { success: false,
│      user: {...} }                message: "Wrong password" }
│
Store client_auth              Show error message
in localStorage                User can retry
│
Redirect to
profile.html
│
Profile loads
user data from
localStorage
```

---

## System Status Summary

```
✅ OPERATIONAL COMPONENTS
═════════════════════════════════════════════════════════════════════════

Component                Status      Description
──────────────────────  ─────────   ──────────────────────────
Backend Server          ✅ Running   Port 3000
Signup Endpoint         ✅ Working   Creates with bcrypt
Login Endpoint          ✅ Working   Authenticates users
Password Reset          ✅ Working   Email verification
Success Page            ✅ Working   Shows email
Email Pre-fill          ✅ Working   Passes via URL param
Auto-register           ✅ Working   Checkout integration
Database                ✅ Working   JSON file with hashed passwords
Validations             ✅ Working   Frontend & backend
Security                ✅ Secure    Bcrypt hashing
Error Handling          ✅ Working   User-friendly messages
```

This completes the full system architecture! ✅
